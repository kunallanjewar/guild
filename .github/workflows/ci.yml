name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  # Cross-compile the full 6-combo matrix from a single ubuntu runner.
  # CGO_ENABLED=0 is part of the product promise (§10.3 — pure-Go SQLite).
  build:
    name: build ${{ matrix.goos }}/${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache: true
      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: go build -trimpath ./...

  # Run tests on each native host (cross-run isn't a thing for go test).
  #
  # This job also runs the daemon parity suite
  # (tests/integration/daemon_parity_test.go): a single Go test that runs
  # the canonical MCP + CLI scenarios twice (daemon-down and daemon-up)
  # in two fresh isolated homes and asserts byte-identical scrubbed
  # output. Both arms therefore run on each host below (ubuntu + macos),
  # which is how the ADR-005 "no-daemon mode stays byte-identical; both
  # modes run the same suite" invariant is enforced in CI. The suite is
  # unix-only (the daemon listens on AF_UNIX); the Windows-tests-excluded
  # note already matches.
  test:
    name: test ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Windows tests are not run — README promises macOS and Linux only.
        # Windows binaries are still cross-compiled in the build job above.
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache: true
      - name: Test
        run: go test -race -count=1 ./...

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache: true
      # Install golangci-lint via go install so it gets compiled with the
      # runner's Go 1.25 toolchain. The prebuilt v1.60 binary from
      # golangci-lint-action was built with Go 1.23 and refuses to lint
      # go.mod's 1.25 target; compiling from source makes the version
      # check agree with the target.
      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.64.8
      - name: Run golangci-lint
        run: golangci-lint run --timeout=5m

  # Custom SQL-safety analyzer (§10b.0b / QUEST-18). Runs as go vet -vettool
  # rather than a golangci-lint plugin (plugin wiring needs a CGO .so — deferred).
  sqlcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache: true
      - name: Build sqlcheck
        run: go build -o sqlcheck ./cmd/sqlcheck
      - name: Run sqlcheck over the full module
        run: ./sqlcheck ./...
