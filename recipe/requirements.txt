# recipe/requirements.txt
#
# Pinned dependencies for the model-build workflow. Bumping any of
# these is a deliberate maintainer action: a push on recipe/** triggers
# .github/workflows/build-model.yml, which rebuilds the int8 ONNX model
# and publishes a new model-v<semver> GitHub Release.
#
# Versions chosen to be recent stable releases as of 2026-Q2. The
# optimum/onnxruntime pair is the load-bearing pin; transformers and
# huggingface_hub are pulled in transitively but pinned here so a
# deterministic recipe run produces a deterministic model artifact.
#
# To regenerate the lockfile-style versions after a bump, see the
# header comment in recipe/quantize.py.

optimum[exporters]==1.24.0
onnxruntime==1.20.0
transformers==4.46.0
huggingface_hub==0.27.0
sentence-transformers==3.3.1
onnxscript==0.1.0
accelerate==1.2.1
